# secretos y estado local
.env
.lampson/
.synsema/
workspace
# notas internas de investigación (no forman parte del producto)
notes/
# memoria por proyecto del agente (local)
memory/
# temporales
tests/*.log
tests/*.txt
*.tmp
